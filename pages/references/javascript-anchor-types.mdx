---
title: Javascript Anchor Types Reference
description: Anchor - Javascript Anchor Types Reference
---

This reference shows you how anchor maps rust types to javascript/typescript types in the client.

export const typeMappings = [
  {
    rustType: "bool",
    jsType: "bool",
    example: "```javascript\nawait program\n  .methods\n  .init(true)\n  .rpc();\n```",
  },
  {
    rustType: "u64/u128/i64/i128",
    jsType: "anchor.BN",
    example: "```javascript\nawait program\n  .methods\n  .init(new anchor.BN(99))\n  .rpc();\n```",
  },
  {
    rustType: "u8/u16/u32/i8/i16/i32",
    jsType: "number",
    example: "```javascript\nawait program\n  .methods\n  .init(99)\n  .rpc();\n```",
  },
  {
    rustType: "f32/f64",
    jsType: "number",
    example: "```javascript\nawait program\n  .methods\n  .init(1.0)\n  .rpc();\n```",
  },
  {
    rustType: "Enum",
    jsType: "object",
    example: "```rust\nenum MyEnum {\n  One,\n  Two { val: u32 },\n  Three(u8, i16),\n};\n```\n```javascript\n// Unit variant\nawait program\n  .methods\n  .init({ one: {} })\n  .rpc();\n\n// Named variant\nawait program\n  .methods\n  .init({ two: { val: 99 } })\n  .rpc();\n\n// Unnamed(tuple) variant\nawait program\n  .methods\n  .init({ three: [12, -34] })\n  .rpc();\n```",
  },
  {
    rustType: "Struct",
    jsType: "{ val: {} }",
    example: "```rust\nstruct MyStruct {\n  val: u16,\n}\n```\n```javascript\nawait program\n  .methods\n  .init({ val: 99 })\n  .rpc();\n```",
  },
  {
    rustType: "[T; N]",
    jsType: "[ T ]",
    example: "```javascript\nawait program\n  .methods\n  .init([1, 2, 3])\n  .rpc();\n```",
  },
  {
    rustType: "String",
    jsType: "string",
    example: "```javascript\nawait program\n  .methods\n  .init(\"hello\")\n  .rpc();\n```",
  },
  {
    rustType: "Vec<T>",
    jsType: "[ T ]",
    example: "```javascript\nawait program\n  .methods\n  .init([1, 2, 3])\n  .rpc();\n```",
  }
];

{<table>
  <thead>
    <tr>
      <th>Rust Type</th>
      <th>Javascript Type</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    {typeMappings.map((item, index) => (
      <tr key={index}>
        <td><pre>{item.rustType}</pre></td>
        <td><pre>{item.jsType}</pre></td>
        <td><pre dangerouslySetInnerHTML={{ __html: item.example }}></pre></td>
      </tr>
    ))}
  </tbody>
</table>}
